@model AT02_4_GaldonMario_Musica.Models.Artist

@{
    ViewData["Title"] = "Delete";
}

<h1 class="text-light">Borrar</h1>

<h3 class="text-light">Estas seguro de que quieres borrarlo?</h3>
<div>
    <h4 class="text-light">Artist</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2 text-light">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10 text-light">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
    <p class="text-warning">@ViewData["Error"]</p>
    <form asp-action="Delete">
        <input type="hidden" asp-for="ArtistId" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Volver a la lista </a>
    </form>
</div>
